/**
 * Webhook Route Handler
 *
 * Handles incoming Telegram webhooks.
 * Re-exported from server.ts for backwards compatibility.
 */

export { createGateway, shutdownGateway, type GatewayOptions, type TelegramUpdate } from '../server.js';
